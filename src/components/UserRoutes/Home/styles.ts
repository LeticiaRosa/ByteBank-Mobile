import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  cardsContainer: {
    // flexDirection: "column",
    flex: 1,
    gap: 16,
    marginBottom: 22,
  },
});
